# Plan 005: Remove stray design scraps committed at the repo root

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md` — unless a reviewer dispatched you and told you they
> maintain the index.
>
> **Drift check (run first)**: `git diff --stat de572482..HEAD -- thinking-woman.png newsletter-mockups.html`
> On a mismatch (e.g. files already deleted), treat as a STOP condition (the
> plan may simply be done already).

## Status

- **Priority**: P3
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: dx
- **Planned at**: commit `de572482`, 2026-07-04

## Why this matters

This branch accidentally committed two working scraps at the repo root: `thinking-woman.png` (25 KB — a draft of the blog mark; the real assets ship as `public/blog-mark.svg`/`.webp`) and `newsletter-mockups.html` (a design mockup page; the shipped email assets live in `public/email/` and are regenerated by `scripts/generate-email-wordmark.mjs`). Nothing imports or serves either file — the only "references" are prose comments mentioning "thinking-woman" as the mark's name (`src/data/site.ts:70,112`, `scripts/generate-email-wordmark.mjs:2`). Root-level orphans confuse future contributors about what is a build input.

## Current state

- `thinking-woman.png` — repo root, added by this branch (`git log --oneline -- thinking-woman.png` shows it landing during the blog-era work).
- `newsletter-mockups.html` — repo root, added by this branch.
- Reference check as of `de572482`: `grep -rn "newsletter-mockups\|thinking-woman" src scripts docs package.json .github` matches only the three prose comments listed above — no imports, no asset pipeline usage.

## Commands you will need

| Purpose   | Command              | Expected on success |
|-----------|----------------------|---------------------|
| Reference check | `grep -rn "newsletter-mockups\|thinking-woman.png" src scripts docs public package.json .github astro.config.mjs wrangler.jsonc` | no matches that are code references (prose mentions of the mark's *name* are fine) |
| Typecheck | `bun run check`      | exit 0              |
| Unit tests| `bun run test:unit`  | all pass            |

Note: this repo lives on a Dropbox mount; sandboxed `bun`/`node` can hit
non-deterministic `EPERM`. If a command fails with `EPERM`, ask the operator to
run it in their own terminal instead of retrying.

## Scope

**In scope**:
- Delete `thinking-woman.png` (repo root)
- Delete `newsletter-mockups.html` (repo root)

**Out of scope** (do NOT touch):
- `public/blog-mark.svg`, `public/blog-mark.webp`, `public/email/**` — the real, shipped assets.
- The prose comments naming "thinking-woman" — they describe the mark, not the file.

## Git workflow

- Work on the current branch (`plan-new-blog-era`) unless the operator says otherwise.
- One commit, message style: `chore: remove stray root design scraps`
- Do NOT push or open a PR unless the operator instructed it.

## Steps

### Step 1: Re-verify zero code references, then delete

Run the reference-check grep from the table. If clean, `git rm thinking-woman.png newsletter-mockups.html`.

**Verify**: `bun run check` → exit 0; `bun run test:unit` → all pass.

## Test plan

The reference grep plus the standard check/test suite is sufficient; these files are not build inputs.

## Done criteria

- [ ] Both files deleted; `git status` shows only the two deletions
- [ ] Reference grep shows no code references
- [ ] `bun run check` and `bun run test:unit` exit 0
- [ ] `plans/README.md` status row updated

## STOP conditions

- The reference grep turns up an actual code reference (import, script src, config path) to either file — report it instead of deleting.
- The files are already gone (plan superseded).

## Maintenance notes

- If the mockup HTML is still wanted as a design artifact, the right home is `docs/` or the email README under `public/email/` — the operator can say so in review; default is deletion.
