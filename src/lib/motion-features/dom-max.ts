// domMax adds drag and layout animation on top of domAnimation. Only
// ProjectStack needs it (card drag); everything else loads the smaller
// dom-animation bundle instead.
export { domMax as default } from 'framer-motion';
