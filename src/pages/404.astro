---
import Layout from '../layouts/Layout.astro';

const title = '404 - page not found';
const description = "The page you're looking for isn't here.";
---

<Layout title={title} description={description} hideSiteNav>
  <main class="not-found">
    <div class="nf-number" data-text="404" aria-hidden="true">404</div>

    <section class="nf-copy" aria-labelledby="not-found-title">
      <p class="nf-kicker">404 / not found</p>
      <h1 id="not-found-title">Page not found</h1>
      <p class="nf-line" data-text="this page slipped out of the index.">this page slipped out of the index.</p>
      <a href="/" class="nf-home">go home</a>
    </section>
  </main>
</Layout>

<style>
  @font-face {
    font-family: 'Geist Mono';
    src:
      url('/fonts/geist-mono-variable.woff2') format('woff2-variations'),
      url('/fonts/geist-mono-variable.woff2') format('woff2');
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }

  :global(body:has(.not-found) .global-header-actions) {
    display: none;
  }

  :global(body:has(.not-found) .site-shell) {
    min-height: 100svh;
  }

  .not-found {
    --nf-bg: hsl(var(--background));
    --nf-fg: hsl(var(--foreground));
    --nf-muted: hsl(var(--muted-foreground));
    position: relative;
    display: grid;
    place-items: center;
    min-height: 100svh;
    overflow: hidden;
    color: var(--nf-fg);
    background: var(--nf-bg);
    cursor: default;
    isolation: isolate;
  }

  .not-found::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }

  .not-found::before {
    opacity: 0.42;
    background-image:
      linear-gradient(hsl(var(--foreground) / 0.05) 1px, transparent 1px),
      linear-gradient(90deg, hsl(var(--foreground) / 0.045) 1px, transparent 1px);
    background-size: 100% clamp(72px, 11svh, 112px), clamp(72px, 11svh, 112px) 100%;
  }

  .nf-number {
    position: relative;
    z-index: 1;
    width: min(92vw, 1100px);
    color: var(--nf-fg);
    font-family: 'Geist Mono', 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: min(38svh, 21rem);
    font-weight: 520;
    line-height: 0.82;
    letter-spacing: -0.03em;
    text-align: center;
    font-variant-numeric: tabular-nums;
    transform: translateY(-1svh);
    transition:
      transform 520ms cubic-bezier(0.16, 1, 0.3, 1),
      letter-spacing 520ms cubic-bezier(0.16, 1, 0.3, 1),
      font-weight 520ms cubic-bezier(0.16, 1, 0.3, 1),
      filter 520ms cubic-bezier(0.16, 1, 0.3, 1);
    will-change: transform;
  }

  .nf-number:hover {
    font-weight: 470;
    letter-spacing: -0.036em;
    filter: contrast(1.08);
    transform: translateY(-1.4svh) scale(1.018);
  }

  .nf-number::before,
  .nf-number::after {
    content: attr(data-text);
    position: absolute;
    inset: 0;
    z-index: -1;
    color: var(--nf-fg);
    pointer-events: none;
  }

  .nf-number::before {
    opacity: 0.075;
    transform: translate(-0.018em, -0.006em);
    animation: nf-callback-a 5.6s ease-out infinite;
  }

  .nf-number::after {
    opacity: 0.04;
    transform: translate(0.024em, 0.008em);
    animation: nf-callback-b 5.6s ease-out infinite;
  }

  .nf-number:hover::before {
    animation: nf-callback-hover-a 1.08s cubic-bezier(0.16, 1, 0.3, 1) infinite;
  }

  .nf-number:hover::after {
    animation: nf-callback-hover-b 1.08s cubic-bezier(0.16, 1, 0.3, 1) infinite;
  }

  .nf-copy {
    position: fixed;
    right: clamp(18px, 2.8vw, 34px);
    bottom: clamp(30px, 4svh, 52px);
    left: clamp(18px, 2.8vw, 34px);
    z-index: 2;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: end;
    gap: 10px 24px;
    pointer-events: none;
  }

  .nf-kicker,
  .nf-copy h1,
  .nf-copy p,
  .nf-home {
    margin: 0;
    font-size: 11px;
    line-height: 1.35;
    letter-spacing: 0;
    text-transform: lowercase;
  }

  .nf-kicker {
    position: fixed;
    top: clamp(82px, 8vw, 118px);
    right: clamp(18px, 4vw, 56px);
    color: var(--nf-muted);
    font-variant-numeric: tabular-nums;
    letter-spacing: 0;
    writing-mode: vertical-rl;
  }

  .nf-copy h1 {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .nf-copy p {
    position: relative;
    max-width: 32ch;
    color: hsl(var(--foreground) / 0.5);
    pointer-events: auto;
  }

  .nf-line::before {
    content: attr(data-text);
    position: absolute;
    left: 0;
    bottom: calc(100% + 8px);
    width: max-content;
    max-width: 32ch;
    color: hsl(var(--foreground) / 0.34);
    opacity: 0;
    pointer-events: none;
    transform: translateY(5px);
    transition:
      opacity 260ms ease,
      transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .not-found:has(.nf-number:hover) .nf-line::before,
  .nf-line:hover::before {
    opacity: 1;
    transform: translateY(0);
  }

  .nf-home {
    position: relative;
    color: var(--nf-fg);
    pointer-events: auto;
    text-decoration: none;
    transition:
      transform 240ms cubic-bezier(0.16, 1, 0.3, 1),
      color 180ms ease;
  }

  .nf-home::before {
    content: "go home";
    position: absolute;
    inset: 0;
    color: hsl(var(--foreground) / 0.22);
    opacity: 0;
    pointer-events: none;
    transform: translate(-0.5em, -0.75em);
    transition:
      opacity 180ms ease,
      transform 240ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .nf-home::after {
    content: "";
    position: absolute;
    right: 0;
    bottom: -4px;
    left: 0;
    height: 1px;
    background: currentColor;
    transform: scaleX(0);
    transform-origin: right center;
    transition: transform 180ms ease;
  }

  .nf-home:hover::after,
  .nf-home:focus-visible::after {
    transform: scaleX(1);
    transform-origin: left center;
  }

  .nf-home:hover,
  .nf-home:focus-visible {
    transform: translateX(-4px);
  }

  .nf-home:hover::before,
  .nf-home:focus-visible::before {
    opacity: 1;
    transform: translate(-0.8em, -1em);
  }

  @media (max-width: 640px) {
    .nf-number {
      width: min(96vw, 420px);
      font-size: min(28svh, 10rem);
      transform: translateY(-4svh);
    }

    .nf-copy {
      grid-template-columns: 1fr;
      align-items: start;
      gap: 8px;
    }

    .nf-kicker {
      top: auto;
      right: auto;
      bottom: 104px;
      left: 20px;
      writing-mode: horizontal-tb;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nf-number::before,
    .nf-number::after {
      animation: none;
    }

    .nf-number,
    .nf-home,
    .nf-home::after {
      transition: none;
    }

    .nf-number:hover {
      font-weight: 520;
      letter-spacing: -0.03em;
      filter: none;
      transform: translateY(-1svh);
    }

    .nf-home:hover,
    .nf-home:focus-visible {
      transform: none;
    }

    .nf-line::before,
    .nf-home::before {
      display: none;
    }
  }

  @keyframes nf-callback-a {
    0%,
    100% {
      opacity: 0.075;
      transform: translate(-0.018em, -0.006em);
    }

    72% {
      opacity: 0.075;
      transform: translate(-0.018em, -0.006em);
    }

    76% {
      opacity: 0.22;
      transform: translate(-0.065em, -0.022em);
    }

    81% {
      opacity: 0.035;
      transform: translate(0.012em, 0.004em);
    }

    86% {
      opacity: 0.075;
      transform: translate(-0.018em, -0.006em);
    }
  }

  @keyframes nf-callback-b {
    0%,
    100% {
      opacity: 0.04;
      transform: translate(0.024em, 0.008em);
    }

    74% {
      opacity: 0.04;
      transform: translate(0.024em, 0.008em);
    }

    78% {
      opacity: 0.16;
      transform: translate(0.075em, 0.024em);
    }

    83% {
      opacity: 0.02;
      transform: translate(-0.01em, -0.004em);
    }

    88% {
      opacity: 0.04;
      transform: translate(0.024em, 0.008em);
    }
  }

  @keyframes nf-callback-hover-a {
    0%,
    100% {
      opacity: 0.12;
      transform: translate(-0.032em, -0.01em);
    }

    34% {
      opacity: 0.2;
      transform: translate(-0.072em, -0.026em);
    }

    58% {
      opacity: 0.055;
      transform: translate(0.018em, 0.006em);
    }
  }

  @keyframes nf-callback-hover-b {
    0%,
    100% {
      opacity: 0.07;
      transform: translate(0.04em, 0.014em);
    }

    32% {
      opacity: 0.16;
      transform: translate(0.09em, 0.03em);
    }

    62% {
      opacity: 0.03;
      transform: translate(-0.014em, -0.005em);
    }
  }
</style>
