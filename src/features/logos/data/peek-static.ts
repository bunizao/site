import { PEEK_BASE } from '@/features/mascot/peek/base';
import type { LogoDefinition } from './types';

export const PEEK_STATIC: LogoDefinition = {
  id: PEEK_BASE.id,
  name: PEEK_BASE.name,
  tagline: PEEK_BASE.tagline,
  blurb: PEEK_BASE.blurb,
  width: PEEK_BASE.width,
  height: PEEK_BASE.height,
  base: PEEK_BASE.base,
  accent: PEEK_BASE.accent,
  animations: {},
};
